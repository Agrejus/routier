Code is TypeScript-first with schema-first API naming. Testing follows integration-first policy from TESTING_SPEC.md: prefer behavior-level integration tests via public APIs, selective unit tests for deterministic logic, avoid hiding failures. Schema coverage constraints currently: primitives, object nesting up to 2 levels, arrays of objects. For docs, follow .cursor/rules/docs.mdc conventions (task-first writing, TS examples, cross-linking, valid YAML/Liquid includes hygiene).