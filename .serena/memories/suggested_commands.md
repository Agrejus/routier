Common commands (Linux/bash): ls, cd <dir>, rg <pattern> <path>, git status, git diff. Monorepo: npm install, npm run build, npm run lint, npm run lint:oxlint, npm run format:oxfmt, npm run format:check:oxfmt, npm run docs:prepare, npm test (within package). Package-level examples: cd core && npm test, cd datastore && npm test, cd react && npm run test. After task completion: run relevant tests/lint/format checks for touched areas and verify no broken scripts.