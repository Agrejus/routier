Readonly collection
    Differential Change Tracking
    Freeze Deep

TODO:
    Deeply nested schemas
    Tests lock up when running in parallel
        Probably something to do with views

Deferred Query using react live useQuery need to return a deferred status when it is deferred, otherwise it will keep loading forever
    Need a way to check and see if it is deferred