Readonly collection
    Freeze Deep
remap property names
    A: added schema.from() <---- Need to implement this

TODO:
    Tracing/obserability is broken
    Can we use a computed property for the ID?  Would be great for history records
    Can we implement a group by?  Would need to happen in memory

    from    
        Saving Data
        CollectionBase.prepare() ->
            Schema prepare then serialize

            New -> prepare, serialize, remap

        Querying Data   
            deserializeAndEnrich
                this calls deserialize then enrich

        SchemaCollection new methods    
            preprocess() -> incorporates prepare then serialize then remap
            postprocess() -> incorporates remap, then deserialize, then enrich

    can we change the API?
        preprocess(...entities) {

            const result = new Array(entities.length);
            for(let i = 0....) {

                // here is the code

            }

            return result

        }