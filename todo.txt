Readonly collection
    Differential Change Tracking
    Freeze Deep

TODO:
    Deeply nested schemas
    Tests lock up when running in parallel
        Probably something to do with views

It might make sense to create a @routier/replication-plugin where we can store Optimistic/HTTP/HTTP + SWR/Replication plugins.
    we cannot put them into core because core does not rely on the memory plugin